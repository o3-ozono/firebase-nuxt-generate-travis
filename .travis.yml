language: node_js
node_js:
  - 10.8.0
install:
  - npm install -g firebase-tools
before_deploy:
  - cd sample
  - npm run generate
  - cd ..
deploy:
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
script:
  - echo "Deploy"
